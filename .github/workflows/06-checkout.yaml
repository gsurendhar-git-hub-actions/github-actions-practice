name: Checkout Code
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    # code checkout will happened
      - name: checkout
        uses: actions/checkout@v5
      - name: display the files
        run: ls -la 